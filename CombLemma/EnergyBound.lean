/-
Copyright (c) 2026 Xinze Li. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
Authors: Xinze Li
-/
import CombLemma.Core

/-!
# `EnergyBound` — re-export stub

As of v0.2 the former `EnergyBound.Refinement` structure and
`exists_reducedEnergy_sup_lt` theorem have been generalized and moved
to `CombLemma.Core` as `FiniteCoverWithWitnesses` and
`exists_sup_reduction_of_cover` (parametric in the near-critical
threshold `δ` and saving floor `ε`).

This file is retained as an import path; see
`CombLemma/Core.lean` for the current definitions.
-/
